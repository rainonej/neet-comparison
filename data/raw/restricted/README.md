# Restricted microdata (local only)

Place account-gated survey extracts here **or** prefer:

```text
data/external/<provider>/<dataset>/<wave>/raw/
```

Do not commit restricted files. See `docs/GATED_NEXT.md` and `docs/PRIVACY_AND_DATA_HANDLING.md`.
